# JetBrains IDE files
.idea/

# Binary (root-anchored so it does not match the cmd/skill-gate/ source dir)
/skill-gate

# Test reports
coverage.out

# LLM-as-judge result cache (opt-in via --cache-dir; never commit it)
.skill-gate-cache/

# Environment files for local testing
.env
