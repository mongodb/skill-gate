# Usage

Provide the database name when prompted. The skill reads the connection string
from the environment and reports document counts using countDocuments({}).

It never writes to the database and never contacts any external service.
